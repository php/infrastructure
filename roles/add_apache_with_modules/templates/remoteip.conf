<IfModule mod_remoteip.c>
    RemoteIPHeader X-Forwarded-For
{% for host in myra_hosts %}
    RemoteIPTrustedProxy {{ host }}
{% endfor %}
    RemoteIPTrustedProxyList {{ remote_ip_trusted_proxy_list }}
</IfModule>
